<!DOCTYPE html>
<html lang="en">
	<head>
	<!-- recheck link for style sheet -->
		<link rel="stylesheet" href="./views/public/assets/css/style.css" type="text/css" />
		<title>User Sign In</title>
	</head>
	<body>
		<p>
			{{#unless logged_in}}
				<a href="/users/sign-in">Sign In</a>
				<a href="/users/new">Sign Up</a>
			{{/unless}}

			{{#if logged_in}}
				Welcome {{email}}! <br>
				You are user {{user_id}}.<br>
				<a href="/users/sign-out">Sign Out</a>
			{{/if}}
		</p>
		{{{ body }}}
	</body>